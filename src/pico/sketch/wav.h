

class Wav {
    long fileSize;
    int numChannels;
    long sampleRate;
    int bitsPerSample;

    long fmtChunkSize;
    int audioFormat;

    long dataChunkOffset;
    long dataChunkSize;

  public:

  private:

  int nextChunk(int currentOffset) {
  }

  int currentChunkSize(int currentOffset) {
  }
}
